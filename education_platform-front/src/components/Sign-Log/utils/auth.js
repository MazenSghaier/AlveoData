export const setToken = (key, value) => {
    // Implement the logic to set the token in localStorage or any other storage mechanism.
    // For example, if using localStorage:
    localStorage.setItem(key, value);
  };